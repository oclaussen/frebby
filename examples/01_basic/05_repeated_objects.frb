# Repeating the same nested key into an object multiple times works
# mostly as you would expect:

foo 'bar' do
  value 'hello'
end

foo 'bar' do
  value 'world'
end

foo 'baz' do
  hello 'world'
end
